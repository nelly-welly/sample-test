!!! info "Key rotation policy"
    PayCart recommends rotating live secret keys every 90 days. Generate the new key in **Dashboard → Developers → API keys → Rotate**, deploy it, then revoke the old key. Pending captures and refunds continue to work for 24 hours after revocation.
