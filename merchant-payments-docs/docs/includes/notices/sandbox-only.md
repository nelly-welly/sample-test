!!! note "Sandbox only"
    The keys, IDs, and amounts shown on this page work only against the **PayCart Sandbox** environment (`https://sandbox.paycart.example`). Production endpoints reject test credentials.
